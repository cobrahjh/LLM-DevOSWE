{
  "name": "terminal-bridge",
  "version": "1.0.0",
  "description": "WebSocket bridge to stream Claude Code terminal output to Command Center",
  "main": "terminal-bridge.js",
  "scripts": {
    "start": "node terminal-bridge.js",
    "dev": "node --watch terminal-bridge.js",
    "lint": "npx eslint *.js --config ../../.eslintrc.json"
  },
  "dependencies": {
    "ws": "^8.14.2"
  }
}
